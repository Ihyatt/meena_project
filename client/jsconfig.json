

{
  "files": [],
  "references": [],
  "compilerOptions": {
    "baseUrl": ".",
      "paths": {
        "src/*": ["./src/*"],
        "@/*": ["./src/*"] // Maps @/ to src/
      }
  },
      "include": ["src/**/*"],  // Only process files in src/
  "exclude": ["node_modules", "**/__tests__"]  // Ignore tests
}